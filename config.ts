export const APP_CONFIG = {
  // Set to true to use the Python Flask Backend
  // Ensure you are running `python backend/app.py` on port 5000
  API_MODE: false,
  
  API_BASE_URL: 'http://localhost:5000/api',
  STORAGE_KEY: 'sso_providers_config'
};